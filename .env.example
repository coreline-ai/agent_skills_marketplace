# Database
DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/skills_marketplace"

# Admin Auth
ADMIN_USERNAME="admin"
ADMIN_PASSWORD_HASH=""  # Generate with: python -c "from passlib.hash import bcrypt; print(bcrypt.hash('your_password'))"
JWT_SECRET_KEY="change-this-in-production-to-a-random-string"
JWT_ALGORITHM="HS256"
JWT_EXPIRE_MINUTES=1440

# GitHub (optional but recommended)
GITHUB_TOKEN=""
GITHUB_API_BASE="https://api.github.com"

# CORS
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# Logging
LOG_LEVEL="INFO"
