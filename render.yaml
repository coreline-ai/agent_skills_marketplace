services:
  # 1. API & Worker Service (Combined for Free Tier)
  - type: web
    name: skills-marketplace-api
    env: docker
    dockerContext: .
    dockerfilePath: Dockerfile.api
    plan: free

    envVars:
      - key: DATABASE_URL
        sync: false
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD_HASH
        sync: false
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: PORT
        value: 10000
    healthCheckPath: /health

  # 2. Web Frontend (Next.js)
  - type: web
    name: skills-marketplace-web
    env: docker
    dockerContext: ./web
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: skills-marketplace-api
          property: host
      - key: INTERNAL_API_URL
        value: http://skills-marketplace-api:10000/api
      - key: PORT
        value: 10000
    healthCheckPath: /health


